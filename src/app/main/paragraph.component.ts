import { Component } from '@angular/core'

@Component({
    selector: 'paragraph',
    template: `<button (click)="getImg(event)">Click me!</button>`,
    styleUrls: ['./main.style.css']
})

export class ParagraphComponent {



}
